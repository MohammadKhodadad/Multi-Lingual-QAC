We introduced a paper skeleton for the JRC-Acquis multilingual legal retrieval benchmark and populated the non-method sections needed for a full draft. The current version frames the benchmark motivation, positions it relative to multilingual retrieval and legal NLP, describes the intended experimental setup, and records the main design trade-offs and limitations while leaving empirical tables open for later insertion.

The next drafting step is to complete the benchmark's `Method` and `Contributions` sections and then replace the placeholder results material with finalized benchmark outputs. Once those pieces are in place, the paper source in `docs/paper/` can become the main narrative document for the project rather than a collection of standalone notes.
